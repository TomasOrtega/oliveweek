[project]
name = "oliveweek"
version = "2.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "playwright==1.62.0",
    "ruff>=0.16.6",
]

[tool.uv]
package = false

[tool.ruff]
include = ["*.py", "*.pyi", "pyproject.toml"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
