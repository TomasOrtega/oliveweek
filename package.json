{
  "name": "oliveweek",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Free local-first meal planning with community recipes and photographs",
  "license": "MIT",
  "engines": {"node": ">=22"},
  "scripts": {
    "build": "node scripts/build.mjs",
    "prestart": "npm run build",
    "start": "node scripts/serve.mjs dist",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "python3 tests/browser_test.py"
  }
}
